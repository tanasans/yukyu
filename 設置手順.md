# 有給休暇 社員閲覧ページ 設置手順

社員がスマホで自分の有給状況を確認するためのページです。サーバー側のプログラムやデータベースは不要で、
**静的ファイルを置くだけ**で動きます。データは合言葉で暗号化してあるため、公開フォルダに置いても
合言葉を知らない人には中身を読めません。

## 置くファイル

```
index.html         … 閲覧ページ（社員に配るURL）
yukyu-secure.js    … 暗号化した有給データ（管理画面で書き出す）
robots.txt         … 検索よけ
```

## 手順

### 1. データを書き出す（管理画面）

1. `有給休暇管理.html` を開く
2. 右側「データ管理」→「社員配信用（暗号化）データを書き出す」を開く
3. 合言葉を入力（社員に伝えるもの。例：`unico2026`）
4. 「yukyu-secure.js を書き出す」→ ファイルが保存される

※ 合言葉はブラウザに記憶され、次回から入力済みの状態になります。

### 2. GitHub Pages で試す場合

1. GitHub で新しいリポジトリを作成（例：`yukyu`）。**Public** で構いません（データは暗号化済み）
2. `index.html` `yukyu-secure.js` `robots.txt` の3つをアップロード（ブラウザ上でドラッグ＆ドロップ可）
3. リポジトリの Settings → Pages → Source を「Deploy from a branch」、Branch を `main` / `(root)` にして保存
4. 数分後 `https://<ユーザー名>.github.io/yukyu/` が使えるようになる
5. このURLと合言葉を社員に一斉配信する

更新するときは、`yukyu-secure.js` を書き出し直して**同じ場所にアップロード（上書き）**するだけです。

### 3. 会社のホームページに移す場合

1. FTP またはレンタルサーバーのファイルマネージャーで、公開フォルダに新しいフォルダを作る
   （例：`/yukyu/`。推測されにくい名前だとより安心です）
2. 3つのファイルをアップロード
3. `https://会社ドメイン/yukyu/` を社員に案内

※ サイトが **https://** であることが必要です（暗号化の解除がブラウザの安全なページでのみ動くため）。
※ WordPress の場合はテーマ内ではなく、ルート直下の別フォルダに置いてください（テーマ更新で消えないように）。

## 社員側の使い方

1. URLを開く → 合言葉を入力（「この端末に記憶する」にチェックしておけば次回から自動）
2. 自分の氏名を選ぶ（次回からは選択済みの状態で開きます。「変更」で選び直せます）
3. 自分の残日数・当年分の内訳・取得状況カレンダーが表示される
4. ブラウザの共有メニュー →「ホーム画面に追加」でアプリのように使えます

## 注意点

- 合言葉は全社員で共通です。氏名を選び直せば他人の状況も見られる仕様なので、
  社内で共有して差し支えない範囲でご利用ください（個人ごとの暗証番号が必要な場合は追加できます）。
- 合言葉を変更したい場合は、新しい合言葉で `yukyu-secure.js` を書き出し直してアップロードしてください。
  社員側は次回アクセス時に再入力が必要になります。
- 表示されるのは書き出した時点のデータです。更新したら書き出し＋アップロードを行ってください。
